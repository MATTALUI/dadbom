{
  "name": "dadbom",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r static/* dist && bun build src --outfile=dist/index.js --minify"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}